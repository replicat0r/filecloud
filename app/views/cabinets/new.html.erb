<%= render "homes/breadcrumbs" %> 


<%= render 'cabinets/form' %>

<% if @cabinet.folder %>
	   <%= link_to "Back to '#{@cabinet.folder.name}' Folder", browse_path(@cabinet.folder) %> 

<% else %>
   <%= link_to "Back", root_url %> 

<% end %>