        
$( ".modal-body" ).html( "<%= j render partial: 'cabinets/form' %>" );
var mediaDropzone = new Dropzone(".filedropzone");

$('.createfolder').modal('show')

   mediaDropzone.on("success", function(file, responseText) {

    setTimeout(function(){
      $('.createfolder').modal('hide');
      _this.removeAllFiles();
    },2500);
  });

